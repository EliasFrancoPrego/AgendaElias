<table class="table">
	<thead class="thead-dark">
		<tr>
      <th scope="col">Id</th>
			<th scope="col">diary</th>
			<th scope="col">Nombre</th>
			<th scope="col">Apellido</th>
			<th scope="col">Teléfono</th>
			<th scope="col">Correo</th>
			<th scope="col">Acciones</th>
		</tr>
	</thead>
	<tbody>
		{% for contact in contacts %}
			<tr>
			  <td>{{contact.id}}</td>
			  <td>{{contact.Tipocontacto | upper }}</td>
			  <td>{{contact.name}}</td>
			  <td>{{contact.lastName}}</td>
			  <td>{{contact.phone}}</td>
			  <td>{{contact.email}}</td>
			  <td>
				  <div class="btn-group" role="group">
					  <a class="btn btn-info text-white" href="{{path('diary.show', {'id': contact.id})}}">
						  Ver </i>
					  </a>
					  <a href="{{path('diary.edit', {'id': contact.id})}}" class="btn btn-light text-black">
						  Editar </i>
					  </a>
					  <a href="{{path('diary.delete', {'id': contact.id})}}" class="btn btn-dark text-white">
						  Eliminar </i>
					  </a>
				  </div>
			  </td>
			</tr>
		{% endfor %}
	</tbody>
</table>
